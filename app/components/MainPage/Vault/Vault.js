import React from 'react';
import PropTypes from 'prop-types';

type Props = {};

function Vault(props: Props) {
  return <div>This is the vault container</div>;
}

Vault.propTypes = {};

export default Vault;
